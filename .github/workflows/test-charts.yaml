name: Helm Test and Lint

on:
    pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout
        uses: actions/checkout@v3

      # Install Helm
      - name: Install Helm
        run: |
          curl https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3 > get_helm.sh
          chmod 700 get_helm.sh
          ./get_helm.sh

      # Lint the Helm charts
      - name: Lint Helm Charts
        run: |
          helm lint charts/api-service
          helm lint charts/n8n-config
          helm lint charts/cronjobs

      # Test the Helm charts
      - name: Test Helm Charts
        run: |
          helm install --dry-run --debug api-service charts/api-service
          helm install --dry-run --debug n8n-config charts/n8n-config
          helm install --dry-run --debug cronjobs charts/cronjobs
